<%= form_with model: species do |form| %>

  <% if species.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(species.errors.count, "error") %> prohibited this species from being saved:</h2>

      <ul>
        <% species.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= form.label :title %></p>
  <p><%= form.text_field :title, class: 'form-text-field' %></p>

  <p  class="mt"><%= form.label :type %></p>
  <p><%= form.number_field :type, class: 'form-text-field' %></p>

  <p class="mt"><%= form.submit "Save", class: 'button button-main' %></p> 
<% end %> 
