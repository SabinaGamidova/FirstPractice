<%= form_with model: urgency do |form| %>
  <% if urgency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(urgency.errors.count, "error") %> prohibited this urgency from being saved:</h2>

      <ul>
        <% urgency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
  <p  class="mt"><%= form.label :title %></p>
  <p><%= form.text_field :title, class: 'form-text-field' %></p>

  <p class="mt"><%= form.submit "Save", class: 'button button-main' %></p> 
<% end %> 

